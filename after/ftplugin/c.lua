vim.opt_local.shiftwidth = 4

-- local utils = require('utils')

-- vim.api.nvim_create_user_command('Compile', function()
-- 	local src = vim.fn.expand("%:p:~")
-- 	local exc = vim.fn.expand("%:p:~:r")
-- 	utils.Compile("gcc", src .. " -o " .. exc .. " && " .. exc)
-- end, { nargs = 0 })
